
trait BinarySearch {
    let mut a: vec!::new();
    fn insert(key: i32) -> bool;
    fn remove(key: i32) -> bool;
    fn search(key: i32) -> i32;
    fn enlarge(size: i32) -> vec!::new();
    fn shrink(size: i32) -> vec!::new();
}

impl BinarySearch for i32 {
    let mut a: vec;
    fn insert(key: i32) -> bool {
        if a.len() == 0 {
            a[0] = key
        }

    }

}

fn main() {

}
